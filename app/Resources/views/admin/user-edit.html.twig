{% extends("base.html.twig") %}
{% block body %}
    <h1>{{ funcao }} usuário</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">{{ funcao }}</button>
    {{ form_end(form) }}
{% endblock %}